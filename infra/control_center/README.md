# QuantPilot Control Center

Start with `cp .env.example .env && docker compose up -d`; stop with `docker compose down` (add `-v` to remove local reporting data). Grafana is read-only and uses PostgreSQL as its sole datasource.
